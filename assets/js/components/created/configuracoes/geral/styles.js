import styled from 'styled-components';
import { Tabs, Card} from 'antd'
const { TabPane } = Tabs

export const Container = styled(Card)`
  
`;

export const ATabs = styled(Tabs)`
  
`;

export const ATabPane = styled(TabPane)`
  
`;
